# data and generated outputs — never commit these
data/
outputs/
*.zip

# python
__pycache__/
*.pyc
.venv/
.ipynb_checkpoints/

# models / caches
*.pt
*.npy

# one-off dataset-audit scratch output (regenerate with the find/comm commands)
expected.txt
present.txt
missing_eval_files.txt
